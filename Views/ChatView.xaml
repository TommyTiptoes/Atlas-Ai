<UserControl x:Class="AtlasAI.Views.ChatView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#FF050508">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" Background="#FF0c0c12" Padding="20,16" BorderBrush="#FF22d3ee" BorderThickness="0,0,0,1">
            <StackPanel>
                <TextBlock Text="💬 Chat" FontSize="24" FontWeight="Bold" Foreground="#FF22d3ee"/>
                <TextBlock Text="AI Assistant Interface" FontSize="14" Foreground="#FF94a3b8" Margin="0,4,0,0"/>
            </StackPanel>
        </Border>
        
        <!-- Content -->
        <Border Grid.Row="1" Padding="20">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="🚀 Chat View" FontSize="32" FontWeight="Bold" Foreground="#FF22d3ee" HorizontalAlignment="Center"/>
                <TextBlock Text="The full Chat interface is available in the separate Chat window." 
                          FontSize="16" Foreground="#FF94a3b8" Margin="0,16,0,0" HorizontalAlignment="Center" TextWrapping="Wrap" MaxWidth="500" TextAlignment="Center"/>
                <TextBlock Text="Use double-click on the avatar or Ctrl+Alt+A to open the Chat window." 
                          FontSize="14" Foreground="#FF64748b" Margin="0,8,0,0" HorizontalAlignment="Center" TextWrapping="Wrap" MaxWidth="500" TextAlignment="Center"/>
                <Button Content="Open Chat Window" 
                        Margin="0,24,0,0" 
                        Padding="16,10"
                        FontSize="14"
                        Background="#FF22d3ee"
                        Foreground="#FF050508"
                        BorderThickness="0"
                        Cursor="Hand"
                        Click="OpenChatWindow_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="#FF67e8f9"/>
                                            </Trigger>
                                            <Trigger Property="IsPressed" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="#FF06b6d4"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
